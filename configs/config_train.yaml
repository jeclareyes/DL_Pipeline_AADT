# configs/config_train.yaml

# Operational profile: standard single-run training/evaluation.
# Usage:
#   python src/train/training_pipeline.py --config-name config_train

# TODO: to implement further and properly with optuna

defaults:
  - config
  - _self_

hydra:
  mode: RUN

training:
  tuning: null
