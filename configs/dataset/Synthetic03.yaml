name: "Synthetic03"
nature: "synthetic"
data_availability:
  has_complete_od_ground_truth: true
  has_observed_link_flows: true
  has_ground_truth_assignment: true
source_format: "tntp"

manifests:
  raw: "data/raw/${dataset.name}/info/dataset_manifest.json"
  processed_default: "data/processed/${dataset.name}/base_manifest.json"

conventions:
  file_stem: "${dataset.name}"
  raw_dir: "data/raw/${dataset.name}"
  processed_dir: "data/processed/${dataset.name}"
